import { Meta, Unstyled } from '@storybook/addon-docs/blocks';


<Meta title="Foundations/Design Tokens" />

# Design Tokens

A range of CSS variables are provided to easily customise the component design across a Tycho-based application.

The default values of these tokens are set in `global.css`, and are in a CSS `@layer` so they can be easily overridden as required.

_List of tokens coming soon._

